1. After unzipping the file / cloning the repo
2. open a terminal
3. npm install
4. make a .env file, paste the text below, and save
	VITE_API_URL=http://pro-ject.api.app.dlsu-lscs.org/
	VITE_LOCAL_API=http://localhost:3000
5. type npm run dev on the terminal to start the front end.